# Enterprise Auth Server — backend

FastAPI application package. See the [project README](../README.md) for full
documentation, architecture, and quick-start instructions.

```bash
uv sync --group dev     # install
uv run pytest           # run the test suite (no external services needed)
uv run uvicorn app.main:app --reload
```
